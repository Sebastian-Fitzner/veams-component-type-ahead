{{#each suggestions }}
	<li class="type-ahead__suggestion-item" role="option">
		<a class="type-ahead__suggestion-link" data-js-item="suggestion-item" href="#" data-value="{{ name }}" aria-controls="{{ instanceId }}"
			aria-expanded="{{ ariaExpanded }}">{{{ suggestionText }}}</a>
	</li>
{{/each}}